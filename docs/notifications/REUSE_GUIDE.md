# 📦 Руководство по переиспользованию модуля уведомлений

Как интегрировать модуль уведомлений в новый проект.

## 🎯 Цель документа

Этот документ поможет вам скопировать и интегрировать модуль уведомлений в любой Vue 3 / Nuxt 3 проект.

---

## 📋 Что нужно скопировать

### Обязательные файлы (6 штук)

#### 1. Composable

```
app/composables/useNotifications.ts
```

**Описание:** Основная логика управления уведомлениями, методы `success()`, `error()`, `warning()`, `info()`.

**Зависимости:** `vue` (ref, readonly)

#### 2. Компонент отображения

```
app/components/NotificationToast.vue
```

**Описание:** Vue компонент для отображения уведомлений с анимациями, стилями и доступностью.

**Зависимости:** 
- `vue` (computed, defineAsyncComponent)
- `useNotifications` (типы)
- `NotificationIcons/*` (компоненты иконок)

#### 3. Компоненты иконок (4 файла)

```
app/components/NotificationIcons/
├── Success.vue      # Иконка успеха (зелёная галочка)
├── Error.vue        # Иконка ошибки (красный крестик)
├── Warning.vue      # Иконка предупреждения (жёлтый треугольник)
└── Info.vue         # Иконка информации (синий круг)
```

**Описание:** SVG иконки для каждого типа уведомления. Загружаются асинхронно.

**Зависимости:** нет

---

## 🚀 Процесс интеграции

### Шаг 1: Подготовка структуры

Создайте папки в целевом проекте (если их нет):

```bash
# Для Nuxt 3
mkdir -p app/composables
mkdir -p app/components/NotificationIcons

# Для Vue 3
mkdir -p src/composables
mkdir -p src/components/NotificationIcons
```

### Шаг 2: Копирование файлов

#### Для Nuxt 3:

```bash
# Из исходного проекта в целевой проект

# Composable
cp source-project/app/composables/useNotifications.ts target-project/app/composables/

# Компонент
cp source-project/app/components/NotificationToast.vue target-project/app/components/

# Иконки
cp -r source-project/app/components/NotificationIcons target-project/app/components/
```

#### Для Vue 3:

```bash
# Composable
cp source-project/app/composables/useNotifications.ts target-project/src/composables/

# Компонент
cp source-project/app/components/NotificationToast.vue target-project/src/components/

# Иконки
cp -r source-project/app/components/NotificationIcons target-project/src/components/
```

### Шаг 3: Адаптация импортов (только для Vue 3)

Если копируете в проект Vue 3 (не Nuxt), измените импорты в `NotificationToast.vue`:

```typescript
// Было (Nuxt):
import { useNotifications } from '~/composables/useNotifications'

// Стало (Vue 3 с алиасами):
import { useNotifications } from '@/composables/useNotifications'

// Или (Vue 3 без алиасов):
import { useNotifications } from '../composables/useNotifications'
```

**Примечание:** В самом `NotificationToast.vue` уже не нужно импортировать `useNotifications` - он получает данные через пропсы.

### Шаг 4: Подключение в корневой файл

#### Для Nuxt 3 - вариант A (app.vue):

```vue
<script setup lang="ts">
import { useNotifications } from '~/composables/useNotifications'

const { notifications, remove } = useNotifications()
</script>

<template>
  <NuxtLayout>
    <NuxtPage />
  </NuxtLayout>
  
  <NotificationToast :notifications="notifications" :on-remove="remove" />
</template>
```

#### Для Nuxt 3 - вариант B (layouts/default.vue):

```vue
<script setup lang="ts">
import { useNotifications } from '~/composables/useNotifications'

const { notifications, remove } = useNotifications()
</script>

<template>
  <div>
    <slot />
  </div>
  
  <NotificationToast :notifications="notifications" :on-remove="remove" />
</template>
```

#### Для Vue 3 (App.vue):

```vue
<script setup lang="ts">
import { useNotifications } from '@/composables/useNotifications'
import NotificationToast from './components/NotificationToast.vue'

const { notifications, remove } = useNotifications()
</script>

<template>
  <div id="app">
    <router-view />
    <NotificationToast :notifications="notifications" :on-remove="remove" />
  </div>
</template>
```

### Шаг 5: Тестирование

Создайте тестовую страницу или компонент:

```vue
<script setup lang="ts">
import { useNotifications } from '~/composables/useNotifications' // или '@/composables/useNotifications'

const { success, error, warning, info } = useNotifications()
</script>

<template>
  <div>
    <h1>Тест уведомлений</h1>
    <button @click="success('Успех!')">Успех</button>
    <button @click="error('Ошибка!')">Ошибка</button>
    <button @click="warning('Предупреждение!')">Предупреждение</button>
    <button @click="info('Информация!')">Информация</button>
  </div>
</template>
```

---

## ✅ Чек-лист интеграции

Используйте этот чек-лист для проверки:

### Файлы

- [ ] `useNotifications.ts` скопирован в `composables/`
- [ ] `NotificationToast.vue` скопирован в `components/`
- [ ] `NotificationIcons/Success.vue` скопирован
- [ ] `NotificationIcons/Error.vue` скопирован
- [ ] `NotificationIcons/Warning.vue` скопирован
- [ ] `NotificationIcons/Info.vue` скопирован

### Подключение

- [ ] Компонент `<NotificationToast />` добавлен в корневой файл
- [ ] Пропс `notifications` передан
- [ ] Пропс `on-remove` передан
- [ ] Импорты адаптированы (для Vue 3)

### Тестирование

- [ ] Тест успеха работает
- [ ] Тест ошибки работает
- [ ] Тест предупреждения работает
- [ ] Тест информации работает
- [ ] Нет ошибок в консоли
- [ ] Уведомления отображаются в правильной позиции
- [ ] Уведомления закрываются автоматически
- [ ] Кнопка закрытия работает

---

## 🔧 Кастомизация после интеграции

### Изменить позицию по умолчанию

```vue
<NotificationToast 
  :notifications="notifications" 
  :on-remove="remove"
  position="top-center"
/>
```

### Изменить размеры

```vue
<NotificationToast 
  :notifications="notifications" 
  :on-remove="remove"
  min-width="250px"
  max-width="600px"
/>
```

### Изменить Z-index

```vue
<NotificationToast 
  :notifications="notifications" 
  :on-remove="remove"
  :z-index="100"
/>
```

### Кастомные цвета через CSS переменные

В вашем глобальном CSS файле:

```css
:root {
  /* Успех */
  --notification-success-bg-rgb: 20, 83, 45;
  --notification-success-border: #15803d;
  --notification-success-text: #dcfce7;
  
  /* Ошибка */
  --notification-error-bg-rgb: 127, 29, 29;
  --notification-error-border: #b91c1c;
  --notification-error-text: #fee2e2;
  
  /* И так далее... */
}
```

Полный список переменных смотрите в [API Reference](./api.md#css-переменные).

---

## 🐛 Возможные проблемы и решения

### Проблема: "Cannot find module '~/composables/useNotifications'"

**Причина:** Неправильный импорт для вашего типа проекта.

**Решение:**
- Для Nuxt 3: `'~/composables/useNotifications'`
- Для Vue 3 с алиасами: `'@/composables/useNotifications'`
- Для Vue 3 без алиасов: `'../composables/useNotifications'`

### Проблема: "Cannot find module 'NotificationIcons/Success.vue'"

**Причина:** Папка с иконками не скопирована или неправильный путь в `NotificationToast.vue`.

**Решение:**
1. Убедитесь, что папка `NotificationIcons/` скопирована в `components/`
2. Проверьте импорты в `NotificationToast.vue`:
   ```typescript
   // Для Nuxt:
   defineAsyncComponent(() => import("~/components/NotificationIcons/Success.vue"))
   
   // Для Vue 3:
   defineAsyncComponent(() => import("@/components/NotificationIcons/Success.vue"))
   ```

### Проблема: Стили не применяются

**Причина:** Конфликт стилей с глобальными CSS или проблема с Teleport.

**Решение:**
- ✅ **Компонент полностью работает без Tailwind CSS** - используются только CSS переменные и кастомные классы
- Проверьте, что нет конфликтов с глобальными стилями (особенно со сбросом стилей)
- Убедитесь, что Teleport работает (целевой элемент существует)
- Проверьте z-index (по умолчанию 50, возможно нужно увеличить)

### Проблема: TypeScript ошибки

**Причина:** TypeScript не находит типы.

**Решение:**
1. Убедитесь, что `tsconfig.json` правильно настроен
2. Перезапустите TypeScript сервер в IDE
3. Проверьте, что все импорты правильные

---

## 📊 Зависимости

### Внешние зависимости

Модуль использует только стандартные API Vue:

```json
{
  "dependencies": {
    "vue": "^3.x"
  }
}
```

### ⚠️ Никаких дополнительных зависимостей!

- ✅ **Tailwind CSS НЕ требуется**
- ✅ **Никаких CSS фреймворков не нужно**
- ✅ Все стили реализованы через кастомные классы и CSS переменные
- ✅ Полностью самодостаточный модуль

### Внутренние зависимости

```
useNotifications.ts
  ├── vue (ref, readonly)
  └── crypto.randomUUID() (Web API)

NotificationToast.vue
  ├── vue (computed, defineAsyncComponent)
  ├── useNotifications (только типы)
  └── NotificationIcons/* (компоненты)

NotificationIcons/*
  └── нет зависимостей
```

---

## 📝 Что НЕ нужно копировать

- ❌ Файлы тестов (если есть)
- ❌ Демо-страницы
- ❌ Документацию (но можете скопировать для себя)
- ❌ Конфигурационные файлы исходного проекта

---

## 🔄 Обновление версии

Если выходит новая версия модуля:

1. Скачайте новые файлы из исходного проекта
2. Сделайте бэкап своих кастомизаций (если есть)
3. Замените старые файлы новыми
4. Проверьте [CHANGELOG.md](./CHANGELOG.md) на наличие breaking changes
5. Протестируйте работу

### Проверка версии

В файле `useNotifications.ts` в комментариях указана версия:

```typescript
/**
 * Composable для управления уведомлениями
 * @version 2.1.0
 * ...
 */
```

---

## 📚 Дополнительные ресурсы

- [⚡ Быстрый старт](./QUICK_START.md) - установка за 3 шага
- [📦 Инструкция по установке](./installation.md) - подробная установка
- [💡 Примеры использования](./usage.md) - практические примеры
- [📚 API Reference](./api.md) - полная документация API
- [📝 Changelog](./CHANGELOG.md) - история изменений

---

## 💡 Советы

1. **Храните оригинальные файлы** - не изменяйте их напрямую, чтобы легко обновлять
2. **Используйте CSS переменные** - для кастомизации вместо изменения компонента
3. **Документируйте изменения** - если что-то меняете, записывайте
4. **Следите за обновлениями** - новые версии могут содержать оптимизации

---

**Версия документа:** 2.1.0  
**Последнее обновление:** Декабрь 2024

**Удачи с интеграцией!** 🚀

