# ⚡ Быстрый старт - Система уведомлений

Максимально быстрая установка модуля уведомлений в ваш проект.

## 📦 Что нужно скопировать

### Для Nuxt 3

Скопируйте эти файлы в ваш проект:

```
app/
├── composables/
│   └── useNotifications.ts
└── components/
    ├── NotificationToast.vue
    └── NotificationIcons/
        ├── Success.vue
        ├── Error.vue
        ├── Warning.vue
        └── Info.vue
```

## 🚀 Установка за 3 шага

### Шаг 1: Скопируйте файлы

Скопируйте все 6 файлов в соответствующие папки вашего проекта.

### Шаг 2: Добавьте компонент

В `app/layouts/default.vue` (или `app/app.vue`):

```vue
<script setup lang="ts">
import { useNotifications } from '~/composables/useNotifications'

const { notifications, remove } = useNotifications()
</script>

<template>
  <div>
    <slot />
  </div>
  
  <!-- Компонент уведомлений -->
  <NotificationToast :notifications="notifications" :on-remove="remove" />
</template>
```

### Шаг 3: Используйте!

В любом компоненте:

```vue
<script setup lang="ts">
import { useNotifications } from '~/composables/useNotifications'

const { success, error, warning, info } = useNotifications()

const handleClick = () => {
  success('Операция выполнена успешно!')
}
</script>

<template>
  <button @click="handleClick">Нажми меня</button>
</template>
```

## ✅ Готово!

Теперь у вас работает система уведомлений! 🎉

## 📚 Дальнейшие шаги

- [Полная документация](./README.md) - все возможности
- [Примеры использования](./usage.md) - практические кейсы
- [API Reference](./api.md) - детали API

## 🎨 Быстрая настройка

### Изменить позицию

```vue
<NotificationToast 
  :notifications="notifications" 
  :on-remove="remove"
  position="top-center"
/>
```

Доступные позиции:
- `top-right` (по умолчанию)
- `top-left`
- `top-center`
- `bottom-right`
- `bottom-left`
- `bottom-center`

### Уведомление с заголовком

```typescript
success('Данные сохранены', {
  title: 'Успех',
  duration: 5000
})
```

### Уведомление с действиями

```typescript
error('Не удалось загрузить', {
  title: 'Ошибка',
  actions: [
    {
      label: 'Повторить',
      variant: 'primary',
      action: async () => {
        await retry()
      }
    }
  ],
  duration: 0
})
```

## ⚠️ Важно: Никаких дополнительных зависимостей!

✅ **Модуль НЕ требует Tailwind CSS**  
✅ **Никаких CSS фреймворков не нужно**  
✅ Все стили реализованы через CSS переменные и кастомные классы

## 🐛 Проблемы?

### Компонент не отображается

1. Убедитесь, что `<NotificationToast />` добавлен в корневой файл
2. Проверьте, что переданы `notifications` и `on-remove`
3. Перезапустите dev сервер

### Ошибка импорта

```typescript
// Для Nuxt 3 используйте:
import { useNotifications } from '~/composables/useNotifications'

// Для Vue 3 используйте:
import { useNotifications } from '@/composables/useNotifications'
// или
import { useNotifications } from '../composables/useNotifications'
```

## 💡 Полезные советы

1. **Критичные ошибки** - используйте `duration: 0` (не закроются автоматически)
2. **Информационные** - используйте `duration: 3000` (3 секунды)
3. **С действиями** - всегда ставьте `duration: 0`

---

**Версия:** 2.1.0  
**Обновлено:** Декабрь 2024

