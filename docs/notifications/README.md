# 🔔 Система уведомлений (Notification Toast)

Универсальная, доступная и оптимизированная система уведомлений для Vue 3 / Nuxt 3 проектов.

## ✨ Особенности

- 🎯 **Семантичные HTML теги** - правильная структура для SEO и доступности
- ♿ **Полная поддержка a11y** - ARIA атрибуты, навигация с клавиатуры
- 🎨 **Настраиваемый дизайн** - 6 позиций, кастомные стили, CSS переменные
- 🚀 **Оптимизированная производительность** - кеширование иконок, правильная очистка таймеров, использование `shallowRef` для массива уведомлений, мемоизация классов
- 📦 **Готов к переиспользованию** - легко интегрируется в любой проект
- 🔧 **TypeScript** - полная типизация
- 🎭 **Расширенные возможности** - заголовки, действия, HTML контент, Teleport
- 🎨 **CSS переменные** - полная кастомизация через CSS переменные

## 🚀 Оптимизации производительности

Модуль включает следующие оптимизации (версия 2.1.0):

- **`readonly()` вместо `computed()`** - более эффективный способ предоставления только для чтения массива уведомлений
- **Компонентная архитектура иконок** - SVG иконки вынесены в отдельные Vue компоненты с асинхронной загрузкой через `defineAsyncComponent()`
- **Устранение дублирования кода** - единая функция `normalizeOptions()` для обработки опций
- **Оптимизированные методы** - однострочные стрелочные функции вместо многострочных для методов типов уведомлений
- **Мемоизация классов** - классы типов уведомлений и иконок хранятся в константах для быстрого доступа
- **Правильная очистка таймеров** - все таймеры корректно очищаются при удалении уведомлений
- **Уменьшение размера бандла** - иконки загружаются асинхронно по требованию

## 📦 Быстрая установка

### Шаг 1: Копирование файлов

Скопируйте следующие файлы в ваш проект:

```
ваш-проект/
├── composables/
│   └── useNotifications.ts
└── components/
    ├── NotificationToast.vue
    └── NotificationIcons/
        ├── Success.vue
        ├── Error.vue
        ├── Warning.vue
        └── Info.vue
```

### Шаг 2: Подключение компонента

#### Для Nuxt 3

```vue
<!-- app/app.vue или app/layouts/default.vue -->
<script setup lang="ts">
import { useNotifications } from '~/composables/useNotifications'

const { notifications, remove } = useNotifications()
</script>

<template>
  <NuxtLayout>
    <NuxtPage />
  </NuxtLayout>
  
  <!-- Компонент уведомлений -->
  <NotificationToast :notifications="notifications" :on-remove="remove" />
</template>
```

#### Для Vue 3 (без Nuxt)

```vue
<!-- App.vue -->
<script setup lang="ts">
import { ref } from 'vue'
import NotificationToast from './components/NotificationToast.vue'
import { useNotifications } from './composables/useNotifications'

const { notifications, remove } = useNotifications()
</script>

<template>
  <div id="app">
    <router-view />
    
    <!-- Компонент уведомлений -->
    <NotificationToast :notifications="notifications" :on-remove="remove" />
  </div>
</template>
```

### Шаг 3: Использование

```vue
<script setup lang="ts">
import { useNotifications } from '~/composables/useNotifications'

const { success, error, warning, info } = useNotifications()

const handleSave = async () => {
  try {
    await saveData()
    success('Данные сохранены успешно!')
  } catch (err) {
    error('Ошибка при сохранении данных')
  }
}
</script>
```

## 🚀 Быстрый старт

### Базовое использование

```typescript
import { useNotifications } from '~/composables/useNotifications'

const { success, error, warning, info } = useNotifications()

// Простые уведомления
success('Операция выполнена успешно!')
error('Произошла ошибка')
warning('Внимание!')
info('Полезная информация')
```

### С кастомной длительностью

```typescript
// Исчезнет через 3 секунды
success('Сообщение', 3000)

// Не исчезнет автоматически
error('Критическая ошибка', 0)
```

## 📑 Дополнительная документация

- [⚡ Быстрый старт](./QUICK_START.md) - установка за 3 шага
- [📝 История изменений](./CHANGELOG.md) - changelog всех версий
- [📦 Инструкция по установке](./installation.md) - подробная установка
- [💡 Примеры использования](./usage.md) - практические примеры
- [📚 API Reference](./api.md) - полная документация API

---

## 📚 Полная документация API

### `useNotifications()`

Composable для управления уведомлениями. Возвращает объект с методами:

#### Методы

| Метод | Описание | Параметры |
|-------|----------|-----------|
| `success()` | Показать уведомление об успехе | `message: string, options?: NotificationOptions \| number` |
| `error()` | Показать уведомление об ошибке | `message: string, options?: NotificationOptions \| number` |
| `warning()` | Показать предупреждение | `message: string, options?: NotificationOptions \| number` |
| `info()` | Показать информационное уведомление | `message: string, options?: NotificationOptions \| number` |
| `show()` | Показать уведомление с указанным типом | `message: string, type: NotificationType, options?: NotificationOptions` |
| `remove()` | Удалить уведомление по ID | `id: string` |
| `clear()` | Очистить все уведомления | - |

#### Реактивные свойства

- `notifications` - Readonly свойство с массивом всех активных уведомлений (оптимизировано с `readonly()` для лучшей производительности)

### `NotificationOptions`

Интерфейс для настройки уведомления:

```typescript
interface NotificationOptions {
  /** Тип уведомления */
  type?: 'success' | 'error' | 'warning' | 'info'
  
  /** Длительность отображения в миллисекундах (0 = не удалять автоматически) */
  duration?: number
  
  /** Заголовок уведомления */
  title?: string
  
  /** Дополнительные действия (кнопки) */
  actions?: NotificationAction[]
  
  /** HTML контент вместо текста (используйте с осторожностью) */
  html?: boolean
  
  /** Не закрывать при клике на уведомление */
  persistent?: boolean
  
  /** Кастомный CSS класс */
  customClass?: string
}
```

### `NotificationAction`

Интерфейс для действий в уведомлении:

```typescript
interface NotificationAction {
  /** Текст кнопки */
  label: string
  
  /** Функция-обработчик (может быть async) */
  action: () => void | Promise<void>
  
  /** Стиль кнопки */
  variant?: 'primary' | 'secondary'
}
```

### `NotificationToast` (компонент)

#### Props

| Prop | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `notifications` | `Notification[]` | **обязательно** | Массив уведомлений для отображения |
| `onRemove` | `(id: string) => void` | **обязательно** | Функция для удаления уведомления по ID |
| `position` | `'top-right' \| 'top-left' \| 'bottom-right' \| 'bottom-left' \| 'top-center' \| 'bottom-center'` | `'top-right'` | Позиция контейнера уведомлений |
| `maxWidth` | `string` | `'500px'` | Максимальная ширина уведомления |
| `minWidth` | `string` | `'300px'` | Минимальная ширина уведомления |
| `zIndex` | `number` | `50` | Z-index контейнера |
| `teleportTo` | `string \| HTMLElement` | `'body'` | Целевой элемент для Teleport |
| `customIcons` | `Partial<Record<NotificationType, CustomIcon>>` | `undefined` | Кастомные иконки для типов уведомлений |
| `containerAriaLabel` | `string` | `'Уведомления'` | Текст для aria-label контейнера |
| `closeButtonAriaLabel` | `string` | `'Закрыть уведомление'` | Текст для aria-label кнопки закрытия |

## 💡 Примеры использования

### Уведомление с заголовком

```typescript
const { success } = useNotifications()

success('Данные успешно сохранены', {
  title: 'Сохранение завершено',
  duration: 5000
})
```

### Уведомление с действиями

```typescript
const { error } = useNotifications()

error('Не удалось загрузить данные', {
  title: 'Ошибка загрузки',
  actions: [
    {
      label: 'Повторить',
      variant: 'primary',
      action: async () => {
        await retryLoad()
      }
    },
    {
      label: 'Отмена',
      variant: 'secondary',
      action: () => {
        console.log('Отменено')
      }
    }
  ],
  duration: 0 // не закрывать автоматически
})
```

### Постоянное уведомление

```typescript
const { warning } = useNotifications()

warning('Важное предупреждение', {
  persistent: true, // не закрывается при клике
  duration: 0 // не исчезает автоматически
})
```

### Обработка ошибок API

```typescript
const { error, success } = useNotifications()

const saveData = async () => {
  try {
    await api.save(data)
    success('Данные сохранены')
  } catch (err) {
    error('Не удалось сохранить данные', {
      title: 'Ошибка сохранения',
      actions: [
        {
          label: 'Повторить',
          variant: 'primary',
          action: async () => {
            await saveData()
          }
        }
      ],
      duration: 0
    })
  }
}
```

### Подтверждение действий

```typescript
const { warning, success } = useNotifications()

const deleteItem = (id: string) => {
  warning('Вы уверены, что хотите удалить этот элемент?', {
    title: 'Подтверждение удаления',
    actions: [
      {
        label: 'Удалить',
        variant: 'primary',
        action: async () => {
          await api.delete(id)
          success('Элемент удален')
        }
      },
      {
        label: 'Отмена',
        variant: 'secondary',
        action: () => {}
      }
    ],
    duration: 0
  })
}
```

### Прогресс операции

```typescript
const { info, success, error, remove } = useNotifications()

const syncData = async () => {
  const notificationId = info('Синхронизация данных...', { duration: 0 })
  
  try {
    await sync()
    remove(notificationId)
    success('Данные синхронизированы')
  } catch (err) {
    remove(notificationId)
    error('Ошибка синхронизации')
  }
}
```

### HTML контент (осторожно!)

```typescript
const { info } = useNotifications()

info('<strong>Важно:</strong> Проверьте настройки', {
  html: true,
  duration: 5000
})
```

### Кастомные стили

```typescript
const { info } = useNotifications()

info('Кастомное уведомление', {
  customClass: 'my-custom-class',
  duration: 5000
})
```

```css
/* В вашем CSS */
.my-custom-class {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

## 🎨 Настройка позиции

```vue
<template>
  <!-- По умолчанию: top-right -->
  <NotificationToast :notifications="notifications" :on-remove="remove" />
  
  <!-- Другие позиции -->
  <NotificationToast 
    :notifications="notifications" 
    :on-remove="remove"
    position="top-left" 
  />
  <NotificationToast 
    :notifications="notifications" 
    :on-remove="remove"
    position="bottom-right" 
  />
  <NotificationToast 
    :notifications="notifications" 
    :on-remove="remove"
    position="bottom-left" 
  />
  <NotificationToast 
    :notifications="notifications" 
    :on-remove="remove"
    position="top-center" 
  />
  <NotificationToast 
    :notifications="notifications" 
    :on-remove="remove"
    position="bottom-center" 
  />
</template>
```

### Кастомные размеры

```vue
<template>
  <NotificationToast 
    :notifications="notifications"
    :on-remove="remove"
    position="top-right"
    min-width="250px"
    max-width="600px"
    :z-index="100"
  />
</template>
```

## 🎯 Типы уведомлений

| Тип | Описание | Использование |
|-----|----------|---------------|
| `success` | Успешная операция | Сохранение данных, успешные действия |
| `error` | Ошибка | Ошибки API, валидации, критичные проблемы |
| `warning` | Предупреждение | Предупреждения, подтверждения |
| `info` | Информация | Общая информация, подсказки |

## ♿ Доступность (a11y)

Компонент полностью поддерживает доступность:

- ✅ Семантичные HTML теги (`<article>`, `role="alert"`)
- ✅ ARIA атрибуты (`aria-live`, `aria-atomic`, `aria-label`)
- ✅ Навигация с клавиатуры
- ✅ Фокус-индикаторы
- ✅ Поддержка скринридеров

### ARIA Live Regions

- `error` уведомления используют `aria-live="assertive"` (немедленное объявление)
- Остальные типы используют `aria-live="polite"` (объявление при паузе)

## 🔧 Кастомизация стилей

### Через CSS переменные

Компонент использует CSS переменные для полной кастомизации. Вы можете переопределить их в вашем CSS:

```css
:root {
  /* Цвета для success */
  --notification-success-bg-rgb: 20, 83, 45;
  --notification-success-border: #15803d;
  --notification-success-text: #dcfce7;
  --notification-success-icon: #4ade80;
  
  /* Цвета для error */
  --notification-error-bg-rgb: 127, 29, 29;
  --notification-error-border: #b91c1c;
  --notification-error-text: #fee2e2;
  --notification-error-icon: #f87171;
  
  /* Цвета для warning */
  --notification-warning-bg-rgb: 120, 53, 15;
  --notification-warning-border: #a16207;
  --notification-warning-text: #fef3c7;
  --notification-warning-icon: #facc15;
  
  /* Цвета для info */
  --notification-info-bg-rgb: 30, 58, 138;
  --notification-info-border: #1e40af;
  --notification-info-text: #dbeafe;
  --notification-info-icon: #60a5fa;
  
  /* Общие настройки */
  --notification-padding: 1rem;
  --notification-border-radius: 0.5rem;
  --notification-gap: 0.5rem;
  --notification-icon-size: 1.25rem;
  --notification-z-index: 50;
  --notification-offset: 1rem;
  --notification-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
}
```

### Через кастомные классы

```typescript
info('Сообщение', {
  customClass: 'bg-purple-900 border-purple-700'
})
```

### Полная переопределение стилей

Скопируйте компонент и измените классы в функции `getNotificationClasses` или используйте CSS переменные для глобальной кастомизации.

## 🎨 Кастомные иконки

Вы можете использовать свои иконки для типов уведомлений:

```vue
<script setup lang="ts">
import { h } from 'vue'
import { useNotifications } from '~/composables/useNotifications'
import NotificationToast from '~/components/NotificationToast.vue'
import MyCustomIcon from '~/components/icons/MyCustomIcon.vue'

const { notifications, remove } = useNotifications()

const customIcons = {
  success: {
    component: MyCustomIcon,
    size: '24px'
  },
  error: {
    component: () => h('svg', { /* ... */ }, [/* ... */]),
    size: '20px'
  }
}
</script>

<template>
  <NotificationToast 
    :notifications="notifications"
    :on-remove="remove"
    :custom-icons="customIcons"
  />
</template>
```

## 🐛 Решение проблем

### Уведомления не отображаются

1. Убедитесь, что компонент `<NotificationToast />` добавлен в корневой компонент
2. Проверьте, что переданы обязательные пропсы `notifications` и `on-remove`
3. Проверьте, что `z-index` достаточен (по умолчанию 50)
4. Убедитесь, что нет конфликтов CSS

### Таймеры не очищаются

Это исправлено в текущей версии. Убедитесь, что используете последнюю версию composable.

### Стили не применяются

1. **Модуль не требует Tailwind CSS** - все стили встроены
2. Проверьте, что нет конфликтов с глобальными стилями
3. Используйте `customClass` для кастомных стилей
4. Используйте CSS переменные для глобальной кастомизации
5. Убедитесь, что компонент правильно добавлен через Teleport

### Teleport не работает

1. Убедитесь, что целевой элемент существует (по умолчанию `body`)
2. Проверьте, что `teleportTo` указан правильно
3. Для кастомного элемента убедитесь, что он доступен в DOM

## 📝 Миграция с предыдущей версии

Если вы обновляетесь со старой версии, старый API продолжает работать:

```typescript
// Старый API (все еще работает)
success('Сообщение', 3000)

// Новый API (рекомендуется)
success('Сообщение', { duration: 3000 })
```

**Важно:** В новой версии компонент требует обязательные пропсы `notifications` и `on-remove`:

```vue
<!-- Старая версия (не работает) -->
<NotificationToast />

<!-- Новая версия (правильно) -->
<script setup>
import { useNotifications } from '~/composables/useNotifications'
const { notifications, remove } = useNotifications()
</script>
<template>
  <NotificationToast :notifications="notifications" :on-remove="remove" />
</template>
```

## 🔄 Интеграция с другими библиотеками

### Vue Router

```typescript
import { useRouter } from 'vue-router'
import { useNotifications } from '~/composables/useNotifications'

const router = useRouter()
const { error } = useNotifications()

router.beforeEach((to, from, next) => {
  if (!isAuthenticated) {
    error('Требуется авторизация', {
      actions: [
        {
          label: 'Войти',
          variant: 'primary',
          action: () => router.push('/login')
        }
      ]
    })
    next(false)
  } else {
    next()
  }
})
```

### Pinia Store

```typescript
import { defineStore } from 'pinia'
import { useNotifications } from '~/composables/useNotifications'

export const useUserStore = defineStore('user', {
  actions: {
    async login(credentials) {
      const { success, error } = useNotifications()
      try {
        await api.login(credentials)
        success('Вход выполнен успешно')
      } catch (err) {
        error('Ошибка входа')
      }
    }
  }
})
```

## 📄 Лицензия

Этот компонент можно свободно использовать в любых проектах.

## 🤝 Вклад

Если вы нашли баг или хотите предложить улучшение, создайте issue или pull request.

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте эту документацию
2. Посмотрите примеры использования в [usage.md](./usage.md)
3. Изучите [инструкцию по установке](./installation.md)
4. Создайте issue с описанием проблемы

---

**Версия:** 2.1.0  
**Последнее обновление:** Декабрь 2024

## 📝 Changelog

### Версия 2.1.0 (Декабрь 2024)

**Оптимизации производительности:**
- ⚡ Заменен `computed()` на `readonly()` для массива уведомлений
- 🎨 SVG иконки вынесены в отдельные компоненты с асинхронной загрузкой
- 🔧 Добавлена функция `normalizeOptions()` для устранения дублирования кода
- ✨ Оптимизированы методы типов уведомлений (однострочные функции)
- 📦 Уменьшен размер кода на ~110 строк

**Структурные изменения:**
- 📁 Новая папка `NotificationIcons/` с компонентами иконок
- 🔄 Использование `defineAsyncComponent()` для ленивой загрузки иконок

**Совместимость:**
- ✅ Полная обратная совместимость с версией 2.0.0
- ✅ API не изменился

### Версия 2.0.0
- Первая стабильная версия с полной функциональностью

